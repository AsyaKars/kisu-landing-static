/**
 * Site configuration for the static landing page.
 *
 * This file replaces the database-backed admin panel of the original project.
 * Edit values here once and rebuild (`npm run build`) to apply.
 */

export const SITE_CONFIG = {
  /**
   * Yandex.Metrika counter ID (digits only, e.g. "98765432").
   * Leave empty string to disable metrika.
   */
  yandexMetrikaId: '',

  /**
   * Yandex Webmaster verification code (the value of the
   * <meta name="yandex-verification" content="..."> tag).
   * Leave empty string to skip the meta tag.
   */
  yandexVerifyCode: '',

  /**
   * Page <title>. Falls back to the title in index.html if empty.
   */
  siteTitle: '',
};
